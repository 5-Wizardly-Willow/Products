const expect = require('chai').expect;
const sinon = require('sinon');

const Products = require('../src/models/productModel.js');

describe('products', () => {
  it('should ', () => {
    sinon.stub(use);
  });
});
